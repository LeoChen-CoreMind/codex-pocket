# 安全策略

## 支持范围

安全修复优先应用于 `main` 分支。当前项目处于早期开发阶段，不承诺维护旧版本分支。

## 报告漏洞

请使用 GitHub 仓库的私密漏洞报告功能，不要公开包含可利用细节、Token、IP、本机路径或私人对话的 issue。报告应包含受影响版本、入口、复现步骤、影响范围和建议修复。

## 部署要求

- 仅在可信局域网使用，公网部署必须额外配置 TLS、访问控制、限流和审计。
- 对非回环监听必须使用随机生成的 Bearer Token，并定期轮换。
- 不要在截图、日志、issue、MCP 配置示例或 shell 历史中泄露 Token。
- Windows 防火墙只开放实际使用的端口和网络配置文件。
- Companion 的窗口绑定与工作区限制属于安全边界，修改时必须进行专项审查。
- 正式 APK 必须由发布者自己的私钥签名，仓库不会提供或接收签名材料。

## 已知边界

拥有 Windows 用户会话权限的本地进程通常可以读取该用户的 Bridge 状态目录。本项目不用于抵御已控制本机账户的攻击者。编辑器、Codex、Node.js、Android 和依赖库的安全更新也需要由部署者及时安装。
